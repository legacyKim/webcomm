; 1. 쪽지함에서 쪽지 크게 보기 ?
; 2. 에러 페이지
; 3. message 박스 금지어 설정.

4. 관리자 페이지 신고수? 같은 것들?
5. 관리자 페이지 toast 적용해 볼까...
6. right_ad 에 따른 너비 값을 관리자페이지에서 유동적으로 적용 + 게시판 제목의 max-width 값도 비례해서 변경.
7. 관리자 페이지 최소한으로 설정해 보자.
8. 확인한 알림의 경우 3일 후 삭제되도록.

작업사항 기록

** seo

** 로그인 처리
export const config = {
  matcher: ["/((?!_next|favicon.ico).*)"], // 모든 경로에 적용 (정적 자산 제외)
};


실제 배포를 해야할 때
1. 공지사항 작성 기능 - 사이트 접속 유저가 있을 때 + 아직 정체성이 없음
2. 로고

3. 에디터 기능
4. 모바일 반응형 작업...

<< 개념정리 >>

1. queryKey, queryClient - boardList.tsx
2. axios 요청 방식
3. db pool 및 prisma
4. nextauth 폴더 내의 코드 분석 + next-auth.d.ts 파일 참조.
5. prisma schema.prisma 파일 분석
    generator client {
    provider = "prisma-client-js"
    output   = "./node_modules/@prisma/client"
    }
6. 마이그레이션
7. dynamic import
8. tiptap immediatelyRender


<< 관리자는 일괄적으로 처리 >>
